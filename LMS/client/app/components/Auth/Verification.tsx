import { styles } from '@/app/styles/style'
import React from 'react'

type Props = {}

const Verification = (props: Props) => {
  return (
    <div className={`text-white ${styles.title}`}>Verification</div>
  )
}

export default Verification